Напишите функцию get_dx(A, B), где A — матрица [[a1, b1], [a2, b2]], а B — список свободных членов [c1, c2]. Функция должна мысленно (или через создание нового списка) заменить первый столбец матрицы A на элементы из B и вернуть полученный определитель