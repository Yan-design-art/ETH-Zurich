#include <iostream>
#include <cmath>

int main() {
	setlocale(LC_ALL, "RU");

	int num, one, two, three, four;

	std::cin >> num;

	one = num / 1000;
	two = (num / 100) % 10;
	three = (num / 10) % 10;
	four = num % 10;

	int sum = one + two + three + four;

	std::cout << "Сумма: " << std::abs(sum);


	return 0;
}